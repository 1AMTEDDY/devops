#before using playbook 
#use terraform to define servers and deploy them
#log into the control server and install ansible
#using an inventory file you define the servers the control server is going to manage
